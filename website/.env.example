# Optional. Raises the GitHub API rate limit when fetching the latest release.
# GITHUB_TOKEN=

# GitHub owner/repo that publishes desktop installers (default kilobyteno/dagr).
# GITHUB_REPO=kilobyteno/dagr
